language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-mcs' ]; then cd .. && eval "mv $currentfolder generator-mcs" && cd generator-mcs; fi
